{
  "name": "datamask-extension",
  "version": "2.1.1",
  "description": "DataMask – AI Data Privacy. Pseudonymisation automatique + prompts structurés.",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "test": "vitest run",
    "test:watch": "vitest",
    "package": "npm run build && cd dist && zip -r ../DataMask-v2.1.1-ChromeStore.zip . && cd .."
  },
  "devDependencies": {
    "jsdom": "^28.1.0",
    "vite": "^7.0.0",
    "vitest": "^4.0.18"
  }
}
